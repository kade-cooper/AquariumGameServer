{
  "name": "aquarium-game-ebs",
  "version": "1.0.0",
  "private": true,
  "description": "Aquarium Game extension backend (EBS) — zero runtime dependencies, Node >= 18 (global fetch).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {}
}
